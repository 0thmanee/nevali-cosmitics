/**
 * Multi-producer marketplace affordances: brand directory, partner sign-up CTAs, “list your brand”, etc.
 * Flip to `true` when onboarding external producer accounts again.
 *
 * When `false`, keep routes and APIs in place — only public navigation and marketing CTAs are hidden.
 */
export const SHOW_MULTI_PRODUCER_EXPERIENCE = false;

/** Seeded / platform-owned public profile slug (`/artisans/[slug]`). */
export const PLATFORM_OWNED_ORG_SLUG = "nevali-cosmetics";
